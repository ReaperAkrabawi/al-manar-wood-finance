{
  "name": "al-manar-wood-finance",
  "private": true,
  "scripts": {
    "start": "npm start --prefix mobile",
    "dev": "npm run dev --prefix mobile",
    "typecheck": "npm run typecheck --prefix mobile",
    "build": "npm run build --prefix mobile"
  }
}
